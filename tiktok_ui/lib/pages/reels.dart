import 'package:flutter/material.dart';

class UserReelsPage extends StatelessWidget {
  const UserReelsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Home"),
    );
  }
}
